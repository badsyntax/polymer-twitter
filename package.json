{
  "name": "polymer-project",
  "version": "0.1.0",
  "description": "An example polymer project",
  "main": "index.html",
  "scripts": {
    "build": "compass compile && vulcanize -o app/build.html app/index.html --csp --inline",
    "start": "npm run build && nodewebkit app/",
    "release": "npm run build && grunt release",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Richard Willis",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-mocha": "^0.4.11",
    "grunt-node-webkit-builder": "^0.1.21",
    "nodewebkit": "^0.9.2-7",
    "vulcanize": "^0.3.0"
  }
}
